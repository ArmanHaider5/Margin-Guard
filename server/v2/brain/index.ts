/**
 * `brain/`'s single public entry point (see `server/v2/README.md`).
 *
 * Not implemented yet — future sprint. Will realize `docs/MGD_V2_BLUEPRINT.md` §4's
 * Root Cause / Recommendation / Opportunity / Confidence / Report Composer engines.
 * Per ADR-006 (`docs/99_ARCHITECTURE_DECISIONS.md`), this is the ONLY module in
 * `server/v2/` permitted to construct an Insight-shaped object (Finding, Root Cause,
 * Recommendation, Opportunity, Risk).
 */
export {};
